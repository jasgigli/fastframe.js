/**
 * FastFrame.js configuration
 */
module.exports = {
  // Plugins
  plugins: [],
  
  // Build options
  build: {
    outDir: 'dist',
    minify: true,
    sourcemap: true
  },
  
  // Dev server options
  server: {
    port: 3000,
    open: true
  }
};
